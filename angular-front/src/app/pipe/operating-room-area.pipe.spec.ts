import { OperatingRoomAreaPipe } from './operating-room-area.pipe';

describe('OperatingRoomAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new OperatingRoomAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
