import { HospitalizedAreaPipe } from './hospitalized-area.pipe';

describe('HospitalizedAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new HospitalizedAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
