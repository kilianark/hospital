import { UrgencyAreaPipe } from './urgency-area.pipe';

describe('UrgencyAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new UrgencyAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
