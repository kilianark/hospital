import { AmbulatoryAreaPipe } from './ambulatory-area.pipe';

describe('AmbulatoryAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new AmbulatoryAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
